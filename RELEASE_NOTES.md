# IsaacLab(火山引擎版)Release Note · v1

本版本是 Isaac Lab 的火山引擎发行版,基于上游 `isaac-sim/IsaacLab` 的 v2.3.2 稳定线
(上游最新的正式版本),镜像内使用 Isaac Sim 5.1。对应分支 `release_v1`。上游的仿真环境、
内置任务与 PPO 训练能力原样保留,可直接使用;以下是在此基础上补充的部分。

---

## 一、SO-ARM101 机械臂支持

上游未收录该机型。本版本集成开源扩展
[`isaac_so_arm101`](https://github.com/MuammerBay/isaac_so_arm101)(BSD-3),提供 reach 与
lift 两个任务,并补充了面向 sim2real 的域随机化配置。

## 二、off-policy 算法(SAC)

上游的内置任务只随附 PPO 训练配置,使用其他算法需要自行集成。SAC 属于 off-policy 算法,
采集到的数据可反复利用,样本效率更高;实测在崎岖地形与人形机器人任务上,最终表现明显
优于 PPO(见「实测数据」)。

训练命令加一个参数即可从 PPO 切到 SAC:`--agent rsl_rl_sac_cfg_entry_point`。覆盖 9 款
足式 / 人形机器人,默认超参为论文原版配置。评估与导出同步支持:可加载 SAC checkpoint
录制评估视频,并导出 `policy.onnx`。

> **出处与本版本的工作。** 算法实现与 Isaac Lab 侧的接线均来自 ETH:算法见
> [`rsl_rl_sac`](https://github.com/leggedrobotics/rsl_rl_sac),Isaac Lab 集成与上述 9 款
> 机器人的配置见其配套 fork [`isaaclab-sac`](https://github.com/sabagian/isaaclab-sac),
> 论文为
> [Bridging the Gap: Enabling Soft Actor Critic for High Performance Legged Locomotion](https://arxiv.org/abs/2605.24975)。
>
> 本版本做的是**版本对齐与交付**:ETH 的实现基于 rsl-rl 4.0.1 并依赖其自有的 IsaacLab
> fork,本版本将 SAC 栈移植到官方 rsl-rl 5.0.1,使其与当前 Isaac Lab 的 PPO 共存于同一套
> 代码,补回移植后失效的评估与 ONNX 导出,并随镜像一并交付 —— 不需要换用某个 fork,也
> 不需要在运行时安装。

## 三、训练画面实时回传

上游的画面串流依赖显卡内置的视频编码硬件(NVENC),而 A100、A30、H20 等主流训练卡并不
具备该硬件,训练过程只能依据日志与奖励曲线判断。本版本采用一条不依赖该硬件的推流链路,
任意训练卡都可以在浏览器中实时查看训练画面。

训练命令加 `--stream` 即可,移动类与机械臂类任务均支持。相机自动跟随机器人群,不需要设置
机位;多个训练同时进行时各占独立房间,互不覆盖。观看页受用户名密码保护。

## 四、训练产物

训练结束后由以下命令产出:

| 命令 | 产出 |
|---|---|
| `make_report.py` | `report.md`:实际生效的全部超参与训练指标 |
| `play.py` | 评估视频 + 可部署模型(`policy.onnx` / `policy.pt`) |
| `upload_tos.py` | 按目录结构上传至 TOS,走内网端点 |

---

## 训练自己的场景

上述内置任务之外,若要训练的场景不在其列,本版本随附 Isaac Sim 工作站作为前置环节:它是
运行在云端 GPU 上的完整 Isaac Sim 编辑器,画面串流到本地客户端,用于拖入资产、调整位姿、
确认布局并保存场景。

场景搭好后需要人工翻译成任务配置。这一步无法自动完成:场景文件只承载几何信息,而奖励、
观测与成功判据必须由人定义。仓库内的 `tasks/pnp` 是一个完整范例 —— 它正由一个在工作站中
搭出的取放场景翻译而来,文件头逐条记录了每个数值的来历:哪些读自场景、哪些量自资产、
哪些依据任务意图确定。

---

## 部署形态

以容器镜像加 Helm chart 交付,拉起即可训练,不需要安装依赖或编译环境。同一个 chart 用
`mode` 区分两种角色:`mode=train` 起训练容器,`mode=render` 起上述工作站。两者可以部署在
不同机器上,搭建场景不占用训练算力。

服务凭据(观看页登录、对象存储密钥)一律经 Kubernetes Secret 注入,不写进部署参数 ——
Helm 会将部署参数原样保存在发布历史中,写在那里等同于留下明文。

---

## 实测数据

### SAC / PPO 对照

A100 单卡、seed 42、各算法推荐配置(PPO 4096 env / SAC 8192 env):

| 任务 | PPO | SAC |
|---|---|---|
| G1 平地 | +27.83 / 26 min(1000 轮) | +29.77 / 49 min(800 轮) |
| G1 崎岖 | +11.54 / 47 min(1500 轮) | **+21.07 / 55 min(800 轮)** |

**困难地形与人形任务上 SAC 明显占优**:G1 崎岖地形上,SAC 训练 28 分钟即达到 PPO 47 分钟的
最终水平,继续训练还能到接近两倍的 reward。平地等简单任务则是 PPO 更快,且质量相当。

> 单 seed、固定预算下的结果,不应外推到任意预算。SAC 的优势在样本效率与最终高度,不在速度
> —— 同轮数下墙钟约为 PPO 的 1.9 倍。

### 并行环境数

| 场景 | 推荐 `--num_envs` | 依据 |
|---|---|---|
| 移动类 PPO | 4096 | 本文基准测试所用配置 |
| 移动类 SAC | 8192(配 800 轮) | ETH 论文协议,已复现论文量级结果 |
| 机械臂操作 PPO | 4096 | |
| 推流确认场景 | 16 | 推流相机按环境创建,不能配大并行度 |

并行度需要调得更高时,注意:

- **SAC 的显存由 replay buffer 决定**(默认 500 万条,全额预分配),环境数的影响反而小。
  显存不足时优先下调 `agent.algorithm.replay_buffer_size`,而不是环境数;
- **机械臂操作任务受物理引擎的材质数量上限约束**,这个上限与显存无关,换更大的卡不会提高;
- **先到顶的不一定是显存,也可能是机器内存**。选卡时要连机器内存一起看,尤其是图形类
  机型,它们配的内存通常远少于训练机型。

---

## 已知边界

| 项 | 说明 |
|---|---|
| SAC 仅支持移动类任务 | 操作类只有 PPO 配置。上游与论文均未提供可参照的操作类 SAC 超参,自行调一组无从验证对错,因此不提供 |
| SAC 不支持断点续训 | replay buffer 不落盘,恢复即冷启动退化 |
| `--stream` 不能配大并行度 | 推流相机按并行环境创建,数量等于 `--num_envs`。用于小规模确认场景,正式训练不加 |
| Sim 工作站必须用带 NVENC 的显卡 | L4 / L20 / RTX 4090 可以,A100 / A30 / H20 不行。无编码器的卡上服务能起、连接能建,但画面永远不来且无有效报错 |
| 工作站同时只接一个客户端 | Isaac Sim 自身限制,多人同时使用需部署独立实例 |

---

## 使用说明

- 命令速查见仓库根目录的 `EXAMPLE_COMMANDS.md`,其中标注了每条命令中哪些位置可以改;
  从搭建场景到产物归档的完整流程见《Physical AI Kit - Isaac Lab 案例实践》。
- 本版本包含上游 IsaacLab v2.3.2 的全部功能,上游完整变更请参考其官方 release notes。
  上游 3.0 系列目前最新为 `v3.0.0-beta2.patch1`,尚无正式版本,本版本暂不跟进。
- 性能数字来自特定硬件与单 seed 的实测,环境数、训练预算与地形难度都会影响结果,
  请以自身场景的实测为准。
